# Normalize line endings to LF in the repo
* text=auto eol=lf

# Lean source files — always LF
*.lean text eol=lf

# JSON/YAML config — always LF
*.json text eol=lf
*.yml  text eol=lf
*.yaml text eol=lf

# Markdown — always LF
*.md text eol=lf

# Binary — no line ending conversion
*.pdf binary
